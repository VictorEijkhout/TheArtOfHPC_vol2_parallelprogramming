// pingpong.c
if (procno==processA) {
  t = MPI_Wtime();
  for (int n=0; n<NEXPERIMENTS; n++) {
    MPI_Send(send_buf,1,MPI_DOUBLE,
    MPI_Recv(recv_buf,1,MPI_DOUBLE,
  }
  /* total time */      t = MPI_Wtime()-t;
  /* per experiment */  t /= NEXPERIMENTS;
  /* single message: */ t /= 2.;
